server:
  port: 9001

spring:
  application:
    name: WYL-SERVICE
  #MySql
  datasource:
    url: jdbc:mysql://wyl-test.c6hn0budlmri.us-east-1.rds.amazonaws.com:3306/indianelection?useSSL=false&serverTimeZone=UTC&useLegacyDatetimeCode=false
    username: admin
    password: 123456678
  #Hibernate
  jpa:
    properties:
      hibernate:
      dialect: org.hibernate.dialect.MySQL5Dialect
      ddl-auto: update

logging:
  level:
    org:
      hibernate:
        SQL: DEBUG

eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      default-zone: http://localhost:8761/erueka
  instance:
    hostname: localhost

#spring.datasource.url=jdbc:mysql://localhost:3306/indianelection?useSSL=false&serverTimeZone=UTC&useLegacyDatetimeCode=false
#spring.datasource.username=root
#spring.datasource.password=

